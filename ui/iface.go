package ui

/*
// Visual is a node in a window's hierarchy.
type Visual interface {
	init(Visual, Visual)

	Init()
	Delete()
	Parent() Visual
	Append(Visual)
	Children() []Visual
	Bounds() image.Rectangle
	SetBounds(image.Rectangle)
	Layout()
}

// Behavior controls an element.
type Behavior interface {
	Mouse(Mouse)
}

// Model manages data.
type Model interface {
}
*/
